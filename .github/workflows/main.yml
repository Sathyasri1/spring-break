Skip to content
Search or jump to…
Pull requests
Issues
Codespaces
Marketplace
Explore
 
@Sathyasri1 
Sathyasri1
/
petep
Public
Cannot fork because you own this repository and are not a member of any organizations.
Code
Issues
Pull requests
Actions
Projects
Security
Insights
Settings
petep/.github/workflows/main.yml
@Sathyasri1
Sathyasri1 Update main.yml
Latest commit 0b92069 2 minutes ago
 History
 1 contributor
27 lines (23 sloc)  876 Bytes
 

# This is a basic workflow to help you get started with boman.ai

name: Bomanai Scanner
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  Boman-scan:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      
      # Runs a single command using the runners shell
      - name: Install packages
        run: sudo pip install -i https://test.pypi.org/simple/ boman-cli-uat==0.22
      - name: Run scan  
        run: sudo boman-cl-uat -a -u run https://devapi.boman.ai/v2/
